module "if-lte-test" {
    version: 1.0
    run {
        set: x:number = 3
        if: x <= 3 {
            log: "x is less or equal to three"
        } else {
            log: "x is greater than three"
        }
    }
}
