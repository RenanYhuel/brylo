module "if-gt-test" {
    version: 1.0
    run {
        set: x:number = 4
        if: x > 3 {
            log: "x is greater than three"
        } else {
            log: "x is not greater than three"
        }
    }
}
